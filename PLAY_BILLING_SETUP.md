# إعداد Google Play Billing — MedVision AI

الخطة: تجربة مجانية 3 أيام، وبعدها اشتراك شهري 50 ج.م أو وصول مدى الحياة 500 ج.م.

## 1) المنتجات المطلوبة في Play Console

من "Play Console ← Monetize (تحقيق الربح) ← Products (المنتجات)" أنشئ منتجين بالضبط بهذه المعرفات:

| النوع | معرف المنتج | السعر | ملاحظات |
|---|---|---|---|
| اشتراك (Subscription) | `premium_monthly` | 50 ج.م / شهر | أضف عرض تجربة "3 أيام مجانًا" |
| منتج داخل التطبيق (INAPP) | `premium_lifetime` | 500 ج.م | دفعة واحدة — وصول مدى الحياة |

### 1.1 إنشاء الاشتراك الشهري `premium_monthly`
1. أنشئ اشتراكًا بالمعرف `premium_monthly`.
2. أضف خطة أساسية (Base plan) بمدة شهر واحد وسعر 50 ج.م (مصر).
3. أضف عرضًا (Offer) على هذه الخطة: **3 أيام مجانًا** (Free trial 3 days).
4. في إعدادات العرض اجعل "Offer tag" يحتوي كلمة `trial` (مثلًا `trial_3d`).

> التطبيق يختار تلقائيًا عرض الاشتراك الذي يحتوي "trial" (من الـ offer tag أو مرحلة سعر مجانية). لو مفيش عرض، هيشتغل الخطة الأساسية العادية.

### 1.2 إنشاء منتج مدى الحياة `premium_lifetime`
1. أنشئ منتج داخل التطبيق (INAPP) بالمعرف `premium_lifetime`.
2. السعر: 500 ج.م — دفعة واحدة.
3. النوع: مدفوع (Paid product) — مش اشتراك.

## 2) التفعيل والاختبار
- ارفع التطبيق لمسار الاختبار (Internal Testing) أولًا.
- أضف نفسك كمُختبِر ترخيص (License Tester) من "Setup ← License testing".
- جرّب الشراء ببطاقة اختبار (Test card) من "Payments profile".
- المنتجات تظهر في التطبيق تلقائيًا بمجرد إنشائها ونشرها على المسار الاختباري.

## 3) ملاحظات فنية
- **التجربة المجانية (3 أيام)**: تتم بالكامل عبر Google Play — التطبيق لا يخزن حالة التجربة محليًا. عند ضغط "ابدأ التجربة المجانية" يفتح Play صفحة الاشتراك ويعرض عرض الـ 3 أيام المجانية (من Play Console)، وعند الموافقة يعتبر Play الاشتراك نشطًا (سواء في فترة التجربة أو بعد الدفع). بعد انتهاء التجربة بدون دفع أو عند الإلغاء، يفقد المستخدم الوصول تلقائيًا لأن التطبيق يسأل Play عند كل تشغيل وعودة للتطبيق.
- **الأسعار**: عند إنشاء المنتجات تظهر أسعارها الرسمية من Play تلقائيًا في صفحة الاشتراك داخل التطبيق.
- **الاشتراك الشهري**: بيتجدد تلقائيًا؛ إدارة/إلغاء الاشتراك من إعدادات Google Play.
- **الاستعادة**: زر "استعادة المشتريات" موجود في صفحة الاشتراك.
- **مصدر الحقيقة**: حالة الاشتراك تُجلب دائمًا من Google Play (عند التشغيل وعند العودة للتطبيق)، والتخزين المحلي مجرد cache ولا يُستخدم أبدًا لمنح المميزات — لو Play قال إن الاشتراك انتهى، بيعتبر منتهي حتى لو الكاش القديم بيقول غير كده.
- **قفل كامل**: التطبيق كله مقفول — كل المميزات (تحليل الصور، دكتور المساعد، المحركات) متاحة فقط مع اشتراك مفعّل أو وصول مدى الحياة. عند فتح التطبيق تظهر شاشة Premium كشاشة قفل لا يمكن تخطيها، وتُفتح المميزات تلقائيًا فور تأكيد Google Play للاشتراك.
- لتجربة صفحة الاشتراك من التطبيق: اضغط "MedVision Premium" في القائمة الجانبية، أو جرّب اختيار "Doctor's Assistant".
